"""merakicore — shared foundation for the Meraki tools.

Modules:
    client    — single Meraki Dashboard API client (reads MERAKI_DASHBOARD_API_KEY)
    networks  — resolve target networks from the live org (by ID, validated)
    io        — save/load helpers for JSON and CSV output
"""
